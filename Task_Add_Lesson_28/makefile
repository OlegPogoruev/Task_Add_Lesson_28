#цель: зависимость
#[tab] команда


BIN = SimLife
SOURCES = main.c
HEADERS = main.h
CC := gcc

all:
	$(CC) $(SOURCES) -lpthread -o $(BIN).out
$(BIN): $(HEADERS)

run: all
	./$(BIN).out
#clean:
#	rm -rf $(BIN) *.o $(BIN).out



















